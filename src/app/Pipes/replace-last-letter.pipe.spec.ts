import { ReplaceLastLetterPipe } from './replace-last-letter.pipe';

describe('ReplaceLastLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceLastLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
